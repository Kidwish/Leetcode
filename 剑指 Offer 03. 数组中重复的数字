class Solution:
    def findRepeatNumber(self, nums: List[int]) -> int:
        tmp=[]
        for i in nums:
            if not(i in tmp):
                tmp.append(i)
            else:
                return i
